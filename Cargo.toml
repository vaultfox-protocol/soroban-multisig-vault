[workspace]
members = ["contracts/vault"]
resolver = "2"

[profile.release]
codegen-units = 1
debug = "line-tables-only"
lto = true
opt-level = "z"
overflow-checks = true
panic = "abort"
strip = "symbols"
